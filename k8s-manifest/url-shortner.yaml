apiVersion: apps/v1
kind: Deployment
metadata:
  name: url-shortner
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: url-shortner
  template:
    metadata:
      labels:
        app: url-shortner
    spec:
      containers:
      - name: url-shortner
        image: manoharshetty507/url-shortner-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 50007
        env:
        - name: DB_HOST
          value: "user-db"
        - name: DB_USER
          value: "user"
        - name: DB_PASSWORD
          value: "userpass"
        - name: DB_NAME
          value: "usersdb"

---
apiVersion: v1
kind: Service
metadata:
  name: url-shortner
  namespace: microservices
spec:
  type: ClusterIP
  selector:
    app: url-shortner
  ports:
  - port: 50007
    targetPort: 50007
